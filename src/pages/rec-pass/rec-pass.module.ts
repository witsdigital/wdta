import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RecPassPage } from './rec-pass';

@NgModule({
  declarations: [
    RecPassPage,
  ],
  imports: [
    IonicPageModule.forChild(RecPassPage),
  ],
})
export class RecPassPageModule {}
